[pytest]
testpaths = tests
addopts = --strict-markers
markers =
    engine: invokes compiled scientific engines or validates their runtime identity
    gui: creates a Windows Tk window; requires an interactive desktop
